use compare::*;
use utils::*;

// For preserving diversity
const MAX_DUPLICATES: usize = 1;
const MAX_SIMILAR: usize = 2;

pub fn select(population_size: usize, indiviuals: Vec<Individual>) -> Vec<Individual> {
    // continue here
    // select population_size individuals from the vector, returning the new, smaller vector
    // select based on fitness, similarity, and prefer smaller individuals
    // first order the individuals by rank, then return the first population_size individuals
    unimplemented!();
}
